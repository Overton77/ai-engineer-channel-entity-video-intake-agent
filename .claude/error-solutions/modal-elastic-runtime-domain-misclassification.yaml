id: "modal-elastic-runtime-domain-misclassification"
created: "2026-08-25T12:43:00Z"
updated: "2026-08-25T12:53:00Z"

error:
  type: "semantic"
  category: "OrganizationDomainMisclassification"
  language: "typescript"
  pattern: "Modal serverless containers and elastic AI runtime classified as horizontal_ai_application"
  context: "Canonical audit of slim-47 Modal developer-experience keynote packet"

diagnosis:
  root_cause: "The schema-valid generic application label survived hydration because Modal lacked an identity-bounded domain refinement."
  factors:
    - "The packet has six first-party Modal homepage, container, snapshot, guide, and SDK sources"
    - "The authoritative organization taxonomy explicitly lists Modal under model_training_inference_platform"

solution:
  immediate:
    - "Add a Modal identity plus serverless/container/GPU/runtime/model-workload evidence refinement"
  proper:
    - "Assign model_training_inference_platform only when official Modal identity and relevant transcript evidence agree"
    - "Guardedly supersede the applied packet after slim-49 owns the local drain"
  code_change: "controller/stages/stage-runner.ts and tests/stateless-pre-research.test.mts"

verification:
  - "Focused Modal regression passes"
  - "Full tests, TypeScript, and production build must pass"
  - "Fresh packet must classify Modal as model_training_inference_platform with twelve hash-matched artifacts and applied operations"

prevention:
  - "Use the repository's explicit taxonomy examples during every organization-domain audit"

metadata:
  occurrences: 1
  last_resolved: "2026-08-25T12:52:40Z"
  success_rate: 1.0
