import { disableTool } from "eve/tools";

// The built-in is disabled globally and reintroduced dynamically by
// stage_network only for the three bounded web-research stages.
export default disableTool();
