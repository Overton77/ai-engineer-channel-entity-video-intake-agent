# Handoff — 2026-08-16 OpenPipe smoke (stopped for disk)

Stopped mid-run so the machine can be cleaned. The last crash was `ENOSPC`. A few GB free is enough to resume if you delete Eve stream logs and leftover sandbox containers first. Watch that `.eve/.workflow-data/streams` does not fill the disk again.

Project: `research_starter_pre_research_agent/`
Eve: `0.38.3` · Model: `zai/glm-5.2`
DB: Supabase `wkythqbofmckbuoothhn`
Operator runbook (generic): [HANDOFF.md](./HANDOFF.md)

---

## What you were doing

Run **one more** eligible video through the v2 controller, verify Postgres + `research-ingestion-intents` ingestion, then write a `.agents/` skill for batch / scheduled runs.

That skill was **not** written. Finish this video first.

---

## Current run (resume this, do not claim a new video)

| Field | Value |
| --- | --- |
| `video_id` | `gEDl9C8s_-4` |
| Title | How to Train Your Agent: Building Reliable Agents with RL — Kyle Corbitt, OpenPipe |
| Duration | 1188s |
| `run_id` | `334e0124-0ea3-4800-8c20-7df4728f7e53` |
| Run status | `synthesizing` |
| Video state | `pipeline_status=synthesizing`, `finished=false` |
| Research session | `wrun_01M06GNNDMHPQJAX86SENXN6KE` (attempt 2, `completed`) |
| Synthesis session | `wrun_01M06JT0E9JPVVTD2M27VAFPDM` (attempt 1, `started`) |
| Registered artifacts | **7 / 12** — `00`–`50` only |

Registered kinds:

`run_manifest, transcript_analysis, taxonomy_classification, web_context, organization_research, source_verification, curriculum_signals`

Missing: `initial_summary`, `technology_library_summary`, `organization_profile`, `ingestion_intent`, `execution_receipt`.

Local copies of `00`–`50` are under:

`outputs/pre-research/v2/gEDl9C8s_-4/334e0124-0ea3-4800-8c20-7df4728f7e53/`

The synthesis sandbox was writing `60`–`90` locally and failing `save_pre_research_packet` on **`IDEMPOTENCY_KEY_MISMATCH`**. Same error as the successful Ramp run (`-rsTkYgnNzM`); the model eventually recomputes the canonical key. Do not start a third synthesis session unless this one is marked failed.

---

## Last finished video (do not rerun)

`-rsTkYgnNzM` / run `8e27309e-a7ec-4624-9225-16c404e17a62` is **applied** and `pre_research_pipeline_finished=true`.

---

## Eligibility (confirmed)

`scripts/eligible-videos.mjs` is the source of truth. A video is eligible when:

- `transcript_status = stored`
- bucket `ai-engineer-transcripts` + `transcript_path` object exists
- `transcript_text` non-empty
- `0 < duration_seconds < 5400` (under 1h 30m)
- no live/applied run for the **current** transcript SHA
- no `pre_research_pipeline_finished` for that SHA

`npm run list:eligible` then uses `first_unprocessed`. After this run finishes, next oldest clean row was `vW8wLsb3Nnc`. Some later catalog rows still have stale `ineligibility_reasons` in `research_pre_research_video_state` even though live SQL predicates pass — trust the list script, not the stale state column.

`TRjq7t2Ms5I` still has a leftover v1 `intent_ready` run. Do not reclaim it.

---

## Parallel / schedules (not started)

Yes: 2–3 `run-pre-research-pipeline.mjs --next` processes can share one Eve server. `claim_pre_research_video` is atomic (`FOR UPDATE SKIP LOCKED`). Do **not** do this until one video finishes cleanly and C: has headroom.

`agent/schedules/process_next_video.ts` is a 30-minute cron that only tells the model to start/resume the controller. It is not a production backfill. Do not enable it until disk + controller wait-loop are stable.

---

## Errors seen on this run (why it felt bad)

1. **Controller treated HITL `waiting` as research done**  
   First pipeline exit after 78s: `RESEARCH_CHECKPOINT_INCOMPLETE` for all `00`–`50`. Root cause: `result()` / stream returned `session.waiting` when the model called `ask_question` for `video_id` + `lease_token`.

2. **Research session asked HITL for `video_id` / `lease_token`**  
   Controller message only had `run_id`. There was no `load_pre_research_run` tool. `touch_pre_research_run` still documents a lease token the controller never gives the model.

3. **`ENOSPC` killed Eve**  
   C: hit 0 bytes. Eve died writing `.eve/.workflow-data/streams` (`195k` files / ~3.6 GB at peak). Docker `docker_data.vhdx` is **64 GB**. After cleanup we had 1–4 GB free; streams grew back to ~2 GB and disk dropped to **271 MB** during synthesis.

4. **`gateway_stream_terminated`**  
   AI Gateway dropped a model stream. Eve parked the session for retry. The controller treated `turn.failed` as a hard research failure and marked the session `RESEARCH_FAILED` even though artifacts were still in progress.

5. **`IDEMPOTENCY_KEY_MISMATCH` on `save_pre_research_packet`**  
   Synthesis wrote `90-ingestion-intent.json` then failed the save tool. Agent was recomputing the canonical key when we stopped. Last Ramp run recovered from this.

6. **`[world-local] Queue delivery failed … TypeError: fetch failed`**  
   Repeated during disk pressure. Usually retried; do not treat as a new video.

7. **`write_file` must-read-before-overwrite**  
   Sandbox noise. Specialists recover.

8. **Stale research session after cutover**  
   After research was marked complete, the old research session tried `save_research_phase_packet` again → `ILLEGAL_PHASE: … got synthesizing`. Harmless.

---

## Code already changed (keep these)

| File | Change |
| --- | --- |
| `agent/agent.ts` | `maxInputTokensPerSession` 2M → **20M**, output 120k → **250k** (last Ramp run needed HITL at 2M/4M) |
| `controller/pre-research-pipeline.ts` | Lease default **10800s**. Phase messages include `video_id`. Wait loop no longer treats `waiting` or `turn.failed` as done. Auto-answers session-limit + video_id questions. Returns completed when required artifact kinds are registered. Falls back to a new research session if attach throws. |
| `agent/tools/load_pre_research_run.ts` | **New.** Load `video_id` from `run_id`. No lease token. |
| `agent/instructions/phase.ts` | Do not call `claim` / `touch`. Do not `ask_question` for run metadata. |
| `scripts/approve-session-limits.mjs` | Sidecar approver (controller now does this itself). |

Still missing / not done:

- `.agents/` batch skill
- Auto-recompute `idempotency_key` inside `save_pre_research_packet` (model should not have to hash it)
- Stream-log rotation / cap on `.eve/.workflow-data/streams`
- Controller reconnect after Eve restart without hanging on a dead session stream

---

## Disk and Docker

C: was **476 GB / 0 bytes free**. There are **no Docker volumes**. Everything below is ephemeral Eve sandbox, not the research checkpoint.

### What the containers are

They are Eve sandbox sessions (`eve-sbx-ses-…`). Each is a throwaway Linux workspace for one root or specialist turn (`__root__`, `transcript_analyst`, `source_verifier`, …). Writable layer is tiny (KB–2 MB). The ~583 MB you see is the shared `ghcr.io/vercel/eve` / `eve-sandbox-template` image, counted once.

When we stopped there were ~22 of them: 8 still Up (including the live synthesis root `wrun_01M06JT0E9JPVVTD2M27VAFPDM`) and the rest `Exited (137)` from the ENOSPC kill. None of them hold the durable packet.

### What you can delete

**Yes — delete all Eve containers.** That does not lose the run. Durable state is:

- Postgres: run `334e0124-…` is `synthesizing`; seven `00`–`50` artifact rows
- Supabase bucket `research-ingestion-intents` for those seven files
- Local `outputs/pre-research/v2/gEDl9C8s_-4/334e0124-0ea3-4800-8c20-7df4728f7e53/`

You **will** lose in-sandbox drafts of `60`–`90` that never passed `save_pre_research_packet`. That is fine. Resume starts a new synthesis session against the saved `00`–`50`.

```bash
# stop Eve first if it is still on :2000
docker rm -f $(docker ps -aq --filter name=eve-sbx-ses)
```

Optional extra (images only, not the run): unused `eve-sandbox-template` tags and leftover k8s desktop images. Do not delete `ghcr.io/vercel/eve:latest` unless you want Eve to re-pull.

### Other reclaim

| Path | Notes |
| --- | --- |
| `research_starter_pre_research_agent/.eve/.workflow-data/streams` | Eve event log. Safe to delete when Eve is **stopped**. Recreated on next `eve dev`. This is what filled the disk, not the containers. |
| `C:\Users\Pinda\AppData\Local\Docker\wsl\disk\docker_data.vhdx` | **64 GB** VM disk. Deleting containers inside Docker does not shrink this file until you compact after `wsl --shutdown`. |
| Recycle Bin / Downloads / Temp | User cleanup. |

```bash
rm -rf research_starter_pre_research_agent/.eve/.workflow-data/streams
mkdir -p research_starter_pre_research_agent/.eve/.workflow-data/streams
```

---

## Resume (after disk is free)

1. Confirm Eve is not already on `:2000`. If it is, stop that terminal first.

```bash
cd research_starter_pre_research_agent
npm exec -- eve dev --no-ui --port 2000
```

Wait for `GET http://127.0.0.1:2000/eve/v1/health` → `{"ok":true}`.

2. Resume **synthesis only** for the existing run (research `00`–`50` are already registered):

```bash
node --experimental-strip-types --import ./scripts/register-ts.mjs \
  scripts/run-pre-research-pipeline.mjs \
  --run-id 334e0124-0ea3-4800-8c20-7df4728f7e53 \
  --synthesis-only
```

If the old synthesis Eve session is dead after the stream wipe, mark that session `failed` (same pattern as research attempt 1) and let the controller start synthesis attempt 2. Do **not** call `claim_pre_research_video` again.

3. When synthesis artifacts exist, a full `--run-id …` (no `--synthesis-only`) applies the intent.

4. Verify:

```bash
npm run query:pre-research -- --video-id=gEDl9C8s_-4
```

Expect `run_status=applied`, `intent_status=applied`, `finished=true`, twelve packet objects, `[ok]` on every artifact hash.

5. Only then write `.agents/skills/run-pre-research/` (batch + schedule). Do not enable concurrency 3 until this video finishes.

---

## Processes when we stopped

- Pipeline: `run-pre-research-pipeline.mjs --run-id 334e0124-…` (killed)
- Eve: `npm exec -- eve dev --no-ui --port 2000` may still be up on **23484**. Stop it before deleting streams or compacting Docker.

---

## Out of scope until asked

- Production schedule
- Concurrency 3
- v1 backfill / `TRjq7t2Ms5I`
- Fetching missing transcripts
